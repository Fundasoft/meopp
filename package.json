{
  "name": "demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rollup -c --environment VIEW:login && rollup -c --environment VIEW:registrar && rollup -c --environment VIEW:panel && rollup -c -w --environment VIEW:index",
    "build": "rollup -c --environment VIEW:login && rollup -c --environment VIEW:registrar && rollup -c --environment VIEW:panel && rollup -c --environment VIEW:index",
    "index": "rollup -c -w --environment VIEW:index",
    "login": "rollup -c -w --environment VIEW:login",
    "registrar": "rollup -c -w --environment VIEW:registrar",
    "panel": "rollup -c -w --environment VIEW:panel",
    "start": "sirv public --no-clear",
    "win32": "server.exe",
    "linux": "./server"
  },
	"devDependencies": {
    "@rollup/plugin-commonjs": "^24.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "rollup": "^3.15.0",
    "rollup-plugin-css-only": "^4.3.0",
    "rollup-plugin-livereload": "^2.0.0",
    "rollup-plugin-svelte": "^7.1.2",
    "svelte": "^3.55.0"
  }
}
